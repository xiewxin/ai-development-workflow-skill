# {{INPUT:測試設計主題}}

> 預設使用能直接執行的最短版本。小型需求用緊湊表格；同一事實只寫一次，其他章節引用情境 ID。無適用類別時只寫「無。依據：…」，不保留空表格。

## 基本資訊／關聯

- 日期／主倉庫／狀態：{{INPUT:YYYY-MM-DD；倉庫識別；草擬｜已核准｜實作中｜已完成｜部分完成｜已阻塞｜已取代}}
- 需求來源／計畫／前版：{{INPUT:相對連結或無＋依據}}
- 本文件回鏈：{{INPUT:已回鏈位置或缺口}}

## Provider 橋接

- 橋接模式／主 Provider／版本：{{INPUT:原生，或目前需求的單一主 Provider；可驗證版本或版本不明限制}}
- 需求／驗收映射：{{INPUT:外部需求、驗收或穩定案例 ID；原生時填本文件}}

| 範圍／倉庫 | 能力／優先級 | 產物相對路徑 | 狀態／完整性 | 唯一可寫所有者 | 讀寫權／同步方向 | 同步結果 |
| --- | --- | --- | --- | --- | --- | --- |
| {{INPUT:需求級或倉庫級範圍}} | {{INPUT:案例、資料、自動化、手動驗證或回填；優先級}} | `{{INPUT:倉庫內相對路徑或無}}` | {{INPUT:完整／缺失／衝突／過時}} | {{INPUT:Provider 或本 Skill}} | {{INPUT:唯讀／可寫；外部產物與本文件間的同步方向}} | {{INPUT:待同步／已同步／阻塞}} |

- 橋接缺口：{{INPUT:資料策略、自動化目標路徑與命令、手動／環境驗證、必要唯讀 SQL、實作後結果、偏差與剩餘風險中尚未覆蓋者；沒有則填無}}

## 範圍與策略

- 規模／目標：{{INPUT:小型｜中型｜大型；可驗證目標}}
- 包含／不包含：{{INPUT:需求明示範圍；排除項目與依據}}
- 策略：{{INPUT:適用的自動化、手動、環境或上線觀察}}
- 額外情境：{{INPUT:無；或安全、資料正確性、主要副作用、高回歸風險＋程式碼證據＋情境 ID}}

## 環境與依賴

- {{INPUT:專案／倉庫；相對執行位置；非正式環境／帳號；依賴與版本；限制，無則填「無。依據：…」}}

## 測試資料策略

| 情境 ID | 專案／倉庫與相對來源 | 建立方式 | 隔離／清理 | 禁止依賴 |
| --- | --- | --- | --- | --- |
| {{INPUT:T-01}} | {{INPUT:倉庫；fixture／factory／seed 路徑}} | {{INPUT:可控資料}} | {{INPUT:rollback／cleanup／無＋依據}} | 正式資料、個資、脆弱固定 ID、偶然環境狀態 |

## 詳細情境

> 先列需求明示情境。只有程式碼證明有安全、資料正確性、主要副作用或高回歸風險時才新增，並在「依據」簡述證據。正常／邊界／無效／異常／權限／相容性／回歸只保留適用者，不為湊類別加案例。
> 優先沿用專案既有尺度；若無：P0＝安全／不可逆資料／服務不可用，P1＝核心驗收／主流程／對外合同，P2＝適用邊界／錯誤／相容性／主要副作用／高回歸，P3＝低風險補充。每個情境都要附排序理由。

| ID／名稱 | 依據 | 前置條件 | 操作步驟 | 可觀察預期 | 類型／優先級 | 回歸保護 |
| --- | --- | --- | --- | --- | --- | --- |
| {{INPUT:T-01／名稱}} | {{INPUT:驗收條件或風險＋相對證據}} | {{INPUT:引用測試資料策略 ID；必要狀態／依賴}} | {{INPUT:可重現步驟}} | {{INPUT:輸出、狀態或副作用}} | {{INPUT:自動化等；專案尺度或 P0-P3＋理由}} | {{INPUT:不變行為}} |

## 目標自動化映射

| 情境 ID | 專案／倉庫；測試檔案／名稱 | mock 與時間／亂數／網路邊界 | 關鍵斷言 |
| --- | --- | --- | --- |
| {{INPUT:T-01}} | `{{INPUT:倉庫；相對路徑／testName}}` | {{INPUT:依賴或無＋依據}} | {{INPUT:輸出、狀態、副作用}} |

- 執行映射：{{INPUT:情境 ID；相對執行位置；預計命令；預期輸出。共用者合併一次}}

不自動化：{{INPUT:無。依據：…；或情境 ID、技術／成本／環境限制與替代驗證}}

## 手動驗證與可選唯讀 SQL

{{INPUT:全部不適用時只寫「無。依據：…」；有適用時只按情境 ID 記錄相對執行位置、權限／環境／帳號、觀察證據與安全限制，操作步驟引用「詳細情境」。唯讀查詢依 Model／Repository／schema 證據提供非正式環境的參數化 SELECT。若需建立或修改資料，只記錄由使用者依目標專案流程另行處理的前置條件與清理要求，不產出 INSERT／UPDATE／DELETE。}}

## 回歸

- 情境內回歸：引用「詳細情境」的 ID，不重述內容。
- 跨情境／完整套件：{{INPUT:回歸範圍、觸發條件與對應情境 ID；預計命令引用「目標自動化映射」，無則填「無。依據：…」}}

## 交付測試清單

| 情境 ID | 交付狀態摘要 |
| --- | --- |
| {{INPUT:T-01}} | {{INPUT:初版僅填未實作／待執行；實作後填精簡狀態，不重述其他章節}} |

## 自動化測試實施結果

{{INPUT:初版只填「尚未進入實作；實作後依情境 ID 回填」。實作後才依 reference 展開精簡表格，記錄實際檔案／名稱、狀態、實際命令／結果、設計偏差及剩餘驗證；資料相同填「依原設計」，有偏差才展開。另記錄經核准不自動化的情境，無則填「無。依據：…」。}}

## 偏差與剩餘風險

- {{INPUT:只記錄跨情境或尚未歸屬的偏差與剩餘風險，引用相關情境 ID，不重複實施結果；全部無則填「無。依據：…」}}
