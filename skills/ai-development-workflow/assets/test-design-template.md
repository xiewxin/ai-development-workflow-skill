# {{INPUT:測試設計主題}}

> 本範本是完整欄位目錄。符合指南「小型原生緊湊檔」條件時，不逐章複製下列完整骨架；合併標題並以 AC-*、T-*、D-*、RUN-* 引用同一事實。標示「條件式」的區塊無對應證據時直接刪除。

## 基本資訊／關聯

- 日期／主倉庫：{{INPUT:YYYY-MM-DD；倉庫識別}}
- 設計狀態：{{INPUT:草擬｜已核准｜已取代}}
- 執行狀態：{{INPUT:未執行｜執行中｜部分通過｜已通過｜已阻塞}}
- 需求來源／計畫／前版：{{INPUT:相對連結、tracker 識別字或無＋依據}}
- 本文件回鏈：{{INPUT:已回鏈位置或缺口}}

## Provider 橋接

- 模式／需求級工作流所有者／版本：{{INPUT:原生；或目前需求的單一工作流所有者與可驗證版本}}
- 原生模式：{{INPUT:本文件擁有詳細測試設計；若不是原生則刪除此列並展開下表}}

### 外部 Provider 映射（條件式：存在測試產物時保留）

| 範圍／倉庫 | 能力／階段能力執行者／測試優先級 | 產物定位 | 狀態／完整性 | 唯一可寫所有者 | 讀寫權／同步方向 | 同步結果 |
| --- | --- | --- | --- | --- | --- | --- |
| {{INPUT:需求級或倉庫級範圍}} | {{INPUT:案例、資料、自動化、手動驗證或回填；執行者；P0／P1／P2／P3 測試優先級}} | `{{INPUT:倉庫內相對路徑、tracker 識別字或無}}` | {{INPUT:完整／缺失／衝突／過時}} | {{INPUT:Provider 或本 Skill}} | {{INPUT:唯讀／可寫；外部產物與本文件間的同步方向}} | {{INPUT:待同步／已同步／阻塞}} |

- 橋接缺口：{{INPUT:外部產物未覆蓋的情境、資料、執行映射、結果或風險；沒有則填無}}

## 範圍與策略

- 規模／目標：{{INPUT:小型｜中型｜大型；可驗證目標}}
- 包含／不包含：{{INPUT:需求明示範圍；排除項目與依據}}
- 策略：{{INPUT:適用的自動化、手動、環境或上線觀察}}
- 測試 seam：{{INPUT:使用者可觀察的最高且穩定公開介面；既有或新增；選擇理由與核准狀態}}
- 驗收映射：{{INPUT:本文件覆蓋的 AC-*；缺口及原因}}
- 額外情境：{{INPUT:無；或安全、資料正確性、主要副作用、高回歸風險＋程式碼證據＋T-*}}

## 環境與依賴

- {{INPUT:專案／倉庫；相對執行位置；非正式環境／帳號；依賴與版本；限制，無則填「無。依據：…」}}

## 測試資料策略

| 資料 ID | 覆蓋情境 | 專案／倉庫與相對來源 | 建立方式 | 隔離／清理 | 禁止依賴 |
| --- | --- | --- | --- | --- | --- |
| `{{INPUT:D-01}}` | `{{INPUT:T-01,T-02}}` | {{INPUT:倉庫；fixture／factory／seed 路徑}} | {{INPUT:可控資料}} | {{INPUT:rollback／cleanup／無＋依據}} | 正式資料、個資、脆弱固定 ID、偶然環境狀態 |

## 詳細情境

> 先列需求明示情境。只有程式碼證明有安全、資料正確性、主要副作用或高回歸風險時才新增。正常／邊界／無效／異常／權限／相容性／回歸只保留適用者。

| ID／名稱 | 對應驗收／依據 | 前置條件／資料 | 操作步驟 | 可觀察預期 | 類型／優先級 | 回歸保護 |
| --- | --- | --- | --- | --- | --- | --- |
| `{{INPUT:T-01}}`／{{INPUT:名稱}} | `{{INPUT:AC-01}}`；{{INPUT:驗收或風險證據}} | `{{INPUT:D-01}}`；{{INPUT:必要狀態／依賴}} | {{INPUT:可重現步驟}} | {{INPUT:輸出、狀態或副作用}} | {{INPUT:自動化等；專案尺度或 P0-P3＋理由}} | {{INPUT:不變行為}} |

## 目標自動化映射

| 情境 ID | 專案／倉庫；測試檔案／名稱 | mock 與時間／亂數／網路邊界 | 關鍵斷言 | 執行 ID |
| --- | --- | --- | --- | --- |
| `{{INPUT:T-01}}` | `{{INPUT:倉庫；相對路徑／testName}}` | {{INPUT:依賴或無＋依據}} | {{INPUT:輸出、狀態、副作用}} | `{{INPUT:RUN-01}}` |

| 執行 ID | 專案／倉庫；相對執行位置 | 預計命令／方式 | 覆蓋情境 | 預期輸出 |
| --- | --- | --- | --- | --- |
| `{{INPUT:RUN-01}}` | `{{INPUT:倉庫；根目錄或相對路徑}}` | `{{INPUT:可重現命令或驗證方式}}` | `{{INPUT:T-01,T-02}}` | {{INPUT:exit code、摘要或可觀察結果}} |

- 不自動化：{{INPUT:無。依據：…；或 T-*、技術／成本／環境限制與替代驗證}}

## 手動驗證與可選唯讀 SQL

{{INPUT:全部不適用時只寫「無。依據：…」；有適用時只按 T-* 記錄相對執行位置、權限／環境／帳號、觀察證據與安全限制，操作步驟引用「詳細情境」。唯讀查詢依 Model／Repository／schema 證據提供非正式環境的參數化 SELECT。若需建立或修改資料，只記錄由使用者依目標專案流程另行處理的前置條件與清理要求，不產出 INSERT／UPDATE／DELETE。}}

## 回歸

- 情境內回歸：引用「詳細情境」的 T-*，不重述內容。
- 跨情境／完整套件：{{INPUT:回歸範圍、觸發條件與對應 T-*；執行方式引用 RUN-*，無則填「無。依據：…」}}

## 交付測試清單

| 情境 ID | 驗證類型 | 執行 ID／方式 | 交付狀態摘要 |
| --- | --- | --- | --- |
| `{{INPUT:T-01}}` | {{INPUT:自動化／手動／環境／上線觀察}} | `{{INPUT:RUN-01 或替代方式}}` | {{INPUT:初版填未實作／待執行；實作後填精簡狀態}} |

## 自動化測試實施結果

{{INPUT:初版只填「尚未進入實作；實作後依 T-* 與 RUN-* 回填」。實作後才展開精簡表格，記錄實際檔案／名稱、狀態、實際命令／結果、設計偏差及剩餘驗證；資料相同填「依 D-* 原設計」，有偏差才展開。另記錄經核准不自動化的情境。}}

## 偏差與剩餘風險

- {{INPUT:只記錄跨情境或尚未歸屬的偏差與剩餘風險，引用相關 T-*，不重複實施結果；全部無則填「無。依據：…」}}
